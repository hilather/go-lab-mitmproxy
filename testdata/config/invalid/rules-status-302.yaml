apiVersion: labmitm.dev/v1alpha1
kind: LabMITM
metadata:
  name: lab-proxy
spec:
  rules:
    enabled: true
    items:
      - id: status-302
        enabled: true
        phase: request
        action:
          type: status
          status: 302
