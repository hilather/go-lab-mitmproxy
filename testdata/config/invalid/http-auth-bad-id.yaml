apiVersion: labmitm.dev/v1alpha1
kind: LabMITM
metadata:
  name: x
spec:
  proxy:
    httpAuth:
      enabled: true
      users:
        - id: Bad_ID
          usernameFile: testdata/config/valid/socks-username
          passwordFile: testdata/config/valid/socks-password
