apiVersion: labmitm.dev/v1alpha1
kind: LabMITM
metadata:
  name: r
spec:
  rules:
    items:
      - id: bad
        phase: websocket
        match:
          opcode: TEXT
        action:
          type: drop
