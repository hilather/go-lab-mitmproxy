apiVersion: labmitm.dev/v1alpha1
kind: LabMITM
metadata:
  name: throttle-small
spec:
  rules:
    items:
      - id: tiny
        phase: response
        action:
          type: throttle
          bytesPerSecond: 255B
