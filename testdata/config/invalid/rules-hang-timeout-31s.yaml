apiVersion: labmitm.dev/v1alpha1
kind: LabMITM
metadata:
  name: lab-proxy
spec:
  rules:
    enabled: true
    items:
      - id: hang-long
        enabled: true
        phase: request
        action:
          type: hang
          hang:
            timeout: 31s
